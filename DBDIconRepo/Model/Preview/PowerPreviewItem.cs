using IconPack.Model;

namespace DBDIconRepo.Model.Preview;

public class PowerPreviewItem : BasePreview
{
    public PowerPreviewItem(string path, PackRepositoryInfo repo) : base(path, repo)
    {
    }
}
