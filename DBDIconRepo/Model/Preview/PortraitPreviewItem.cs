using IconPack.Model;

namespace DBDIconRepo.Model.Preview;

public class PortraitPreviewItem : BasePreview
{
    public PortraitPreviewItem(string path, PackRepositoryInfo repo) : base(path, repo)
    {
    }
}
