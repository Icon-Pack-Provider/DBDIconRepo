using IconPack.Model;

namespace DBDIconRepo.Model.Preview;

public class OfferingPreviewItem : BasePreview
{
    public OfferingPreviewItem(string path, PackRepositoryInfo repo) : base(path, repo)
    {
    }
}
