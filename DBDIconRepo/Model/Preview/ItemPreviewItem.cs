using IconPack.Model;

namespace DBDIconRepo.Model.Preview;

public class ItemPreviewItem : BasePreview
{
    public ItemPreviewItem(string path, PackRepositoryInfo repo) : base(path, repo)
    {
    }
}
