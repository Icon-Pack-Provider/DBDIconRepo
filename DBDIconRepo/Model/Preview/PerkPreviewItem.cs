using IconPack.Model;

namespace DBDIconRepo.Model.Preview;

public class PerkPreviewItem : BasePreview
{
    public PerkPreviewItem(string path, PackRepositoryInfo repo) : base(path, repo)
    {
    }
}
